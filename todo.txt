#1 priority match system (Call it MatchBox?)
#2 priority FireScope (scouting other robots and running analysis)
#3 priority FireWatch (scouting our own robot and analys of its data on the field live and in general)

whiteboard system
ai suggestion

how data is organized
dictionary of teams
per team is an array of matches in each match is pairs

in the case i made a cheap form to create nodes develop an actual ui to set them up

program blows up if selected blank is null

if network performance becomes an issue make it so that instead of each node fetching data have the manager fetch it and pass it into each node it makes

how about we actually add a node creation system first

also throw in some css to make it look pretty

WHAT IS SENDING SO MANY REQUESTS ITS FREEZING THE COMPUTER

find what keeps spamming requests (they are all called MatchBox if that helps)